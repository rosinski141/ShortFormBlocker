*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
build/
/captures
.externalNativeBuild
.cxx
local.properties

# Release signing. Never commit these: anyone holding the keystore can ship an
# "update" to this app that phones already trust. Back them up somewhere private -
# losing them means friends must uninstall before they can install a new version.
/release-keystore.jks
/keystore.properties
*.jks
*.keystore
